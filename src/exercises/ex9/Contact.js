// src/exercises/ex9/Contact.js
import React from 'react';

export default function Contact() {
  return (
    <div style={{ textTransform: 'lowercase', color: 'var(--text)' }}>
      <h2 style={{ margin: '0 0 8px 0', fontWeight: 'normal' }}>contact</h2>
      <p style={{ margin: 0, color: 'var(--subtext)' }}>
        imagine a form here. for now, it’s just quiet pixels and good intent.
      </p>
    </div>
  );
}
